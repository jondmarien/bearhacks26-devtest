{
  "name": "bearhacks26-monorepo",
  "version": "1.0.0",
  "scripts": {
    "dev": "bun concurrently \"bun run dev:backend\" \"bun run dev:frontend\"",
    "dev:backend": "cd backend && bun run dev",
    "dev:frontend": "cd frontend && bun run dev",
    "install:all": "cd backend && bun install && cd ../frontend && bun install"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
